project2-1_design_document

               ARGUMENT PASSING
               ================

---- DATA STRUCTURES ----

>> A1: Copy here the declaration of each new or changed `struct' or
>> `struct' member, global or static variable, `typedef', or
>> enumeration.  Identify the purpose of each in 25 words or less.
1. We used no struct, global, static, typedef, or enumeration for argument passing.


---- ALGORITHMS ----

>> A2: Briefly describe how you implemented argument parsing.  How do
>> you arrange for the elements of argv[] to be in the right order?
>> How do you avoid overflowing the stack page?
1. By using strtok_r(), we was able to get argc and argv[i]'s address.
2. We arranged elements to be in the right order by pushing arguments closest to argc.
3. We lowered esp by exact size to the data to be pushed, thus never going beyond PHYS_BASE.

---- RATIONALE ----

>> A3: Why does Pintos implement strtok_r() but not strtok()?
1. First, there was no strtok() function.
2. Also, from research, it seemed strtok() used static argument, thus possibility of race condition.

>> A4: In Pintos, the kernel separates commands into a executable name
>> and arguments.  In Unix-like systems, the shell does this
>> separation.  Identify at least two advantages of the Unix approach.
1. Less load to the program since it doesn't have to parse the input
2. program is more secure since if the program can do "ab, cde, abd, de", 
and if there was no shell, then the input would be abcde, 
then there is danger in program possible of doing (ab, cde), or (abc, de).

                 SYSTEM CALLS
                 ============

---- DATA STRUCTURES ----

>> B1: Copy here the declaration of each new or changed `struct' or
>> `struct' member, global or static variable, `typedef', or
>> enumeration.  Identify the purpose of each in 25 words or less.
1. In thread.h : 
struct filedata : has arguments of int for fd, file* to save the file accoring to the fd, 
list_elem to save it in list fds, and char* for remove operation.
2. In struct thread : 
    struct thread* parent : to identify parent thread
    struct list children : to save what thread is the child
    struct list_elem am_child : used by parent to save this thread is the child
    struct semaphore withchild : used to wait when parent need child to synchronize
    struct semaphore withparent : used to wait when child need parent to synchronize
    int initialized : used in thread_create() to initialize semaphore, children list
    int exitval : to save its exit status
    struct list fds : used as list of files + their fd
    int availablefd : used to give the files their fd
3. In syscall.c : 
static struct semaphore filesema : to ensure critical section(file) when doing open, create, close

>> B2: Describe how file descriptors are associated with open files.
>> Are file descriptors unique within the entire OS or just within a
>> single process?

1. Since this is single-threaded process, and each thread has unique struct list fds, 
the file descriptors are unique just within a single process.

---- ALGORITHMS ----

>> B5: Briefly describe your implementation of the "wait" system call
>> and how it interacts with process termination.
1. process_wait() can know the child_tid is the real child thread, since each thread has a list of children tid.
2. use semaphore to increase the child's semaphore, and lower mine. then parent wait until the child exits.
3. use semaphore linked with exit to ensure that the child doesn't exit before parent's process_wait(), and 
ensure that the parent achieved the child's exit status.
4. and then, child process terminate and parent's wait system call return.



==== Peer Evaluation ====

+----------------------------+---------------------------------+

|            Name            |          Contribution        |

+----------------------------+---------------------------------+

| Ho-Jun Lee                 | 50%, Implemented user memory access, system call

+----------------------------+---------------------------------+

| Seong-Ji Yang              |  50%, Implemented argument passing, system call

+----------------------------+---------------------------------+